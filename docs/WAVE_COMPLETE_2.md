# Safe capability wave

Completed read-only system diagnostics, workspace-scoped file inspection, bounded HTTP(S) retrieval, device identity contracts, tool metadata introspection, and capability catalog alignment.

Arbitrary shell/Python execution and broad browser/desktop/device control remain disabled.