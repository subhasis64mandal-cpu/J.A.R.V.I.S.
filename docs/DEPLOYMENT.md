# Local deployment

Run the Python runtime with `python main.py` from the repository root. The Home Base bridge binds to loopback. The web/file/system tools are read-only and bounded. Optional voice dependencies remain optional.
