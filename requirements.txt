# J.A.R.V.I.S. core
# The runtime still has a standard-library fallback when optional AI/voice stacks are absent.
google-genai>=1.0,<2

# Optional voice stack
SpeechRecognition>=3.10,<4
pyttsx3>=2.90,<3
# PyAudio is platform/audio-backend dependent and will be handled separately.
# PyAutoGUI, BeautifulSoup, and Selenium are intentionally NOT installed yet.
